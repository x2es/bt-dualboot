#!/usr/bin/env bash

FIX_KEYWORDS=('mark.only' 'TODO' 'DEBUG' 'debug_shell' 'breakpoint')

for keyword in ${FIX_KEYWORDS[@]}; do
  ag $keyword .
done

bin/pre-release/lint-all
bin/start-tests-all --flags pre-release $@

